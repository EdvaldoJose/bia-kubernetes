apiVersion: v1
kind: Service
metadata:
  name: bia-kubernetes-service
spec:
  type: NodePort
  selector:
    app: bia-kubernetes
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
      nodePort: 30007